FROM nginx:1.30.4-trixie

# El contenido HTML no se copia acá: se monta en runtime con -v
# sobre /usr/share/nginx/html (ver readme.md).

# Documenta el puerto 80 del contenedor
EXPOSE 80

# Este es el comando por defecto que ejecuta el contenedor
# CMD ["nginx", "-g", "daemon off;"]
